Public Class Constants
    Public Shared Pi = 3.1415926535897931

    Public Shared Function GetPi(decimals As Long) As Double
        Dim numerator As Double = 1
        Dim denominator As Long = 3
        Dim result As Double = (2 * decimals) / (2 * decimals + 1)

        While decimals >= 1

        End While

        Return result
    End Function
End Class
